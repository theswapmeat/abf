# Lead-capture endpoints. Unset in development — forms log their payload to
# the console and resolve as successful so the UI can be built/tested before
# a real backend exists. Point these at your CRM/marketing platform for prod.
PUBLIC_LEAD_FORM_ENDPOINT=
PUBLIC_NEWSLETTER_ENDPOINT=
